#include <stdio.h>
#include <conio.h>
void main()
{ // variable and datatype

    clrscr();

    int number = 454;
    float pi = 3.14;
    char persontage = '#';

    printf("%d", number);
    scanf("%d,%d,%d", &number, &pi, persontage);
    getch();
}
